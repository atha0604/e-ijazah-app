# ============================================
# APLIKASI NILAI E-IJAZAH - ENVIRONMENT CONFIG
# ============================================

# SECURITY WARNING: NEVER commit the actual .env file to git!
# Copy this file to .env and fill with your secure values

# JWT Secret Key (REQUIRED)
# Generate a secure random string using one of these methods:
# Method 1 (Node.js): node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Method 2 (OpenSSL): openssl rand -hex 64
# Method 3 (Online): https://www.random.org/strings/ (length: 128, digits+upper+lower)
JWT_SECRET=GENERATE_A_SECURE_RANDOM_STRING_HERE_MINIMUM_64_CHARACTERS

# Server Port (Optional - default: 3000)
PORT=3000

# CORS Allowed Origins (Required for production)
# Separate multiple origins with comma
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# For development, you can use: http://localhost:3000,http://localhost:3001
CORS_ORIGINS=http://localhost:3000

# Database Configuration (Optional - defaults to src/database/db.sqlite)
# DB_PATH=./src/database/db.sqlite

# Node Environment (development | production | test)
NODE_ENV=production
